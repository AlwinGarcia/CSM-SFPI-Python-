# Handles user input expressions.
# - Cleans up math syntax (e.g., ^ → **, ln → log).
# - Validates that only safe functions are used.
# - Compiles the string into a callable Python function g(x) or f(x).
# Connects to: main.py, cli.py, tkinter_ui.py (all user input flows start here).
