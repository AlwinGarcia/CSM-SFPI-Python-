# Marks this folder as a Python package so we can import modules easily.
# We can also re-export key functions here (like iterate_until_converged, check_convergence)
# so that main.py or the UI can import them directly without digging into submodules.

