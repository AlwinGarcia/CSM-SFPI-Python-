# Builds a structured iteration table for display.
# - Each row includes iteration number, xi, xi+1, error %, and optionally g’(xi).
# - Used to show results in terminal (CLI) or GUI.
# Connects to: iteration.py (receives iteration history), ui/cli.py or ui/tkinter_ui.py (for output).
